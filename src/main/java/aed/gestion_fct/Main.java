package aed.gestion_fct;

/**
 * Clase principal que contiene el método {@code main}, punto de entrada de la aplicación.
 * Esta clase se encarga de crear una instancia de {@link GestionApp} e iniciar la ejecución
 * de la aplicación.
 */
public class Main {

    /**
     * Método principal que se ejecuta al iniciar la aplicación.
     * Crea una instancia de la clase {@link GestionApp} y llama al método 
     * {@link GestionApp#iniciarAplicacion()} para iniciar la aplicación.
     * 
     * @param args los argumentos pasados al programa, si los hay (en este caso no se utilizan).
     */
    public static void main(String[] args) {
        // Crear una instancia de la clase GestionApp
        GestionApp gestionApp = new GestionApp();
        
        
    }
}
